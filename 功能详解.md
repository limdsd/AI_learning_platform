# AI 智能学习平台 —— 功能详解

> 每个功能讲清楚三件事:**作用(干什么)、实现(怎么做)、技术点(用了什么)**。
> 配合代码对照看,面试照着讲就能讲透。

---

## 一、整体架构:一个请求怎么走完的

```
前端 Axios → AuthInterceptor(验 JWT)→ Controller(接参数)→ Service(业务逻辑)
                                    → Mapper(MyBatis-Plus 操作 MySQL)
                                    → DeepSeekClient(需要 AI 时)
```

- **Controller** 只做「收参数、调 service、包成统一返回体」,不含业务逻辑。
- **Service** 是真正干活的地方,所有业务规则都在这里。
- **Mapper** 继承 MyBatis-Plus 的 `BaseMapper`,增删改查几乎不用手写 SQL。
- 所有接口统一返回 `Result<T>{code, message, data}`,异常统一由 `GlobalExceptionHandler` 兜底。

---

## 二、认证模块(注册/登录/权限)

### 1. 注册 `AuthService.register()`
- **作用**:创建账号,密码加密入库。
- **实现**:先查用户名是否已存在 → 密码用 **BCrypt** 加密(`encode()`,存的是密文不是明文)→ 建号后直接签发 JWT,实现「注册即登录」。
- **技术点**:BCrypt 自带盐 + 慢哈希,专门抗暴力破解,不可逆。

### 2. 登录 `AuthService.login()`
- **作用**:校验账号密码,通过后发 token。
- **实现**:按用户名查用户 → `passwordEncoder.matches(明文, 密文)` 比对(是「比对」不是「解密」)→ 通过后生成 token 返回。

### 3. JWT 工具 `JwtUtil`
- **作用**:生成/解析/校验 token,实现无状态登录。
- **实现**:`generateToken` 把 userId 放 subject、username 放 claim、设 24 小时过期、HS384 签名;`getUserId` 验签取回 userId;`validate` 验签失败或过期返回 false。
- **技术点**:JWT 本身携带身份 + 签名,服务端不存 session;缺点是签发后无法主动作废,所以设短有效期。

### 4. 拦截器 `AuthInterceptor`
- **作用**:给登录/注册外的接口加「门禁」。
- **实现**:放行 OPTIONS(跨域预检)→ 取 `Authorization: Bearer <token>` → 校验通过则把 userId 塞进 UserContext 放行,失败返回 401。

### 5. 用户上下文 `UserContext`
- **作用**:用 `ThreadLocal` 保存「当前请求是谁」。
- **实现**:拦截器进来时 `setUserId`,请求结束 `afterCompletion` 里 `clear()`;Service 层 `getUserId()` 直接拿,不用层层传参。
- **技术点**:ThreadLocal 保证线程隔离不串号;用完必须 clear,否则线程池复用会串数据。

---

## 三、AI 调用模块(核心,面试重点)

### 1. `DeepSeekClient`
- **作用**:封装 DeepSeek 的 OpenAI 兼容接口,一个方法搞定「发消息、拿回复」。
- **实现**:`chat()` 方法组装 `model + messages(system/user) + temperature`;**`jsonMode=true` 时加 `response_format:{type:"json_object"}` 强制模型只输出 JSON**;用 `RestClient` POST 到 `/chat/completions`;`extractContent()` 从 `choices[0].message.content` 抠出文字。
- **技术点**:没用 Spring AI / LangChain4j,手写 HTTP 调用,提示词/格式/容错全掌控;`response_format` 强制 JSON 是「约束 AI 输出」的典型做法。

### 2. `PromptTemplate`
- **作用**:三类提示词模板(出题/解析/判分)。
- **实现**:
  - 出题:规定角色 + 输出 JSON 结构 + 每种题型怎么填(单选 4 选项、多选答案逗号分隔、判断固定对错、填空/简答 options 为空)。
  - 解析:拼「题干+选项+答案」,让模型讲思路/知识点/易错点。
  - 判分:拼「题目+参考答案+学生作答」,要求输出 `{"score":0,"comment":"..."}` 并限制分数范围。
- **技术点**:提示词工程 = 明确角色 + 明确格式 + 边界约束。

### 3. `QuestionParser`
- **作用**:把 AI 返回的 JSON 变成 Java 对象,并做容错。
- **实现**:
  - `parseGeneratedQuestions`:遍历 `questions` 数组逐题取字段,标记 `source=AI`,缺 content 的题跳过,最后为空抛异常。
  - `parseJudgeResult`:解析 `{score, comment}`,用 `Math.max(0, Math.min(fullScore, score))` 把分数钳制在合法范围。
  - `extractJson`:找第一个 `{` 到最后一个 `}`,去掉 AI 可能包裹的 markdown 代码块。
- **技术点**:「强制 JSON + 解析 + 字段校验」三步串起来,就是「AI 输出不稳定如何兜底」的完整答案。

---

## 四、场景一:智能生成题目

### `QuestionService.generateByAI()`
- **作用**:输入知识点/题型/难度/数量 → AI 出题 → 自动入库。
- **流程**:生成提示词 → `deepSeekClient.chat(..., true)` 强制 JSON → `questionParser.parseGeneratedQuestions()` 解析 → 循环 `insert` 批量入库(标记 `source=AI`)。
- 配套:手动新增 `save()`、分页 `page()`(LambdaQueryWrapper 条件过滤)、更新 `update()`、逻辑删除 `delete()`。

---

## 五、场景二:智能刷题

### 1. 抽题 `getQuestions()`
- **作用**:随机抽 N 道题给用户刷。
- **实现**:按知识点/难度过滤 → `ORDER BY RAND() LIMIT n` 随机抽 → 返回时 `toVOWithoutAnswer()` **隐藏答案和解析**(否则抽题就暴露答案了)。

### 2. 提交判分 `submit()`
- **作用**:判断对错、写记录、答错进错题本。
- **流程**:`JudgeUtil.judge()` 判对错 → 写 `practice_record` → 答错则 `recordWrong()` 入错题本 → 返回对错 + 正确答案 + 解析。

### 3. 判分工具 `JudgeUtil`
- **作用**:客观题本地判分,零成本零延迟。
- **实现**:
  - `isObjective()`:single/multiple/judge 是客观题,fill/short_answer 是主观题。
  - `judge()`:单选/判断去空格后忽略大小写比对;多选把两边答案按逗号拆成**集合**比对,所以 `"A,C"` 和 `"C,A"` 算对。
- **技术点**:这是「混合判分」设计的一半。

### 4. 错题本 `WrongQuestionService.recordWrong()`
- **作用**:记录错题,重复错次数 +1。
- **实现**:先查该用户+该题是否已存在;不存在插入(`wrongCount=1`),已存在 `wrongCount+1` 并重置 `mastered=0`。

### 5. AI 智能解析 `aiExplain()`
- **作用**:对单题生成通俗讲解。
- **实现**:查题 → 拼提示词 → 调 AI(**jsonMode=false**,解析是纯文字不需要 JSON)。

### 6. 个性化推荐 `recommend()`
- **作用**:根据错题找出薄弱知识点,优先推荐同类题。
- **实现(闭环核心)**:查未掌握错题 → 按知识点分组统计出错最多的知识点 → 抽该知识点题目(排除已做过)推荐 → 无错题则退化随机推荐。
- **技术点**:「错题 → 知识点 → 推荐」闭环,可解释、不依赖复杂算法。

---

## 六、场景三:智能考试

### 1. AI 组卷 `generate()`
- **作用**:AI 出题 → 创建试卷 → 关联题目。
- **流程**:AI 出题入库 → `score = 100/题数` 每题均分,写 `exam` → 循环插 `exam_question`(examId + questionId + score + sort,多对多中间表)。

### 2. 开始考试 `start()`
- **作用**:创建「进行中」考试记录。
- **实现**:写 `exam_record` 状态 `ongoing`、记 `startTime`,返回记录 id。

### 3. 交卷 `submit()`(最复杂)
- **作用**:客观自动判分 + 主观 AI 判分 + 汇总 + 错题入库。
- **流程**:
  1. 校验:记录存在、属于当前用户、状态仍 `ongoing`(防重复交卷);
  2. 答案整理成 `questionId → userAnswer` 的 Map;
  3. 遍历每题:**客观题**本地判分(对得满分,错 0 分 + 入错题本);**主观题**调 `judgeSubjective()` 让 AI 打分给评语;每题写 `exam_answer`;
  4. 汇总客观分 + 主观分 = 总分,状态改 `submitted`;
  5. 返回成绩报告。

### 4. 主观题 AI 判分 `judgeSubjective()`
- **作用**:AI 给填空/简答打分写评语。
- **实现**:未作答直接 0 分(不浪费 AI 调用);有作答拼提示词 → 调 AI 强制 JSON → `parseJudgeResult` 解析,分数钳制在 0~满分。

### 5. 成绩报告 `report()`
- **作用**:查记录,组装逐题结果 + 总分。
- **实现**:查记录 → 查试卷题目 → 查作答明细 → `buildReport()` 拼出每题「用户答案/正确答案/得分/解析/AI 评语」+ 总分/客观分/主观分。

---

## 七、通用层(工程细节)

| 组件 | 作用 | 技术点 |
|---|---|---|
| `Result<T>` | 统一返回 `{code,message,data}` | 前端好处理,不用到处判 |
| `GlobalExceptionHandler` | 全局异常兜底 | `@RestControllerAdvice`,业务/校验/格式异常分类处理 |
| `BizException` | 业务异常 | 抛出后统一转友好提示 |
| 逻辑删除 | 删除只改 `deleted=1` | MyBatis-Plus `@TableLogic` 自动过滤 |
| 雪花 ID | 主键自动生成 | `IdType.ASSIGN_ID`,全局唯一趋势递增 |
| 分页 | `Page` + `PageResult` | MyBatis-Plus 分页插件 |
| CORS / Vite 代理 | 解决跨域 | 开发期 Vite proxy `/api` → 8081 |

---

## 一句话串起来(面试复述用)

> 用户注册登录拿到 JWT,之后每个请求带 token 过拦截器,userId 存进 ThreadLocal。三大场景里,「出题」和「组卷」是提示词模板 + 强制 JSON + 解析入库;「刷题」和「考试」的客观题用 JudgeUtil 本地判分、主观题才调 AI 判分,答错自动进错题本;「推荐」靠错题按知识点统计找薄弱点。整个项目没套 AI 框架,RestClient 手写调用,JSON 解析加了容错。
