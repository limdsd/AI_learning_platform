# 面试问答手册(AI 智能学习平台)

> 面向「Java 后端 / 全栈 / 校招」岗位。目标:让你能**用自己的话**讲清楚这个项目,扛住 HR 和技术面试。
> 使用建议:先通读一遍,再对着每道题**开口复述**,不要死记硬背。

---

## 一、项目一句话介绍(电梯陈述)

> 这是一个 AI 驱动的智能学习平台,前后端分离,围绕「智能出题、智能刷题、智能考试」三大场景。后端用 Spring Boot 4 + MyBatis-Plus + MySQL,AI 部分我**没有用现成框架,而是直接用 RestClient 调 DeepSeek 的 OpenAI 兼容接口**,实现了 AI 自动出题、主观题 AI 判分、错题智能解析和薄弱点推荐。

**面试官可能接的话:**「为什么不用 Spring AI / LangChain4j?」
> 回答:这个项目我本来就想把调用链搞清楚,手写 HTTP 调用能让我完全掌控提示词、JSON 格式和容错逻辑,依赖也更轻;引入框架反而会多一层抽象。而且 DeepSeek 本身就是 OpenAI 兼容的 HTTP 接口,封装一个 Client 类就够了。

---

## 二、HR 常问(考察态度与表达)

### Q1. 简单介绍一下你自己
> 模板:我是 XX,计算机相关专业(或自学转行),做过一个 AI 智能学习平台项目,前后端独立完成。技术栈是 Spring Boot 4 + Vue 3 + MySQL,核心是三个 AI 学习场景。我在这个项目里完整走了一遍从需求分析、数据库设计、后端接口到前端联调的全流程,对后端分层、接口设计、AI 接入有实际经验。

### Q2. 你为什么做这个项目?
> 回答要点:**痛点驱动**。传统刷题平台题目是死的,不能按我的薄弱点出题、也没有针对性的解析。我想做一个「输入一个知识点就能 AI 出题、刷完错题能自动归纳薄弱点、考试主观题能被 AI 打分」的平台,把「学 - 练 - 测」闭环用 AI 串起来。顺便把学到的 Spring Boot、MyBatis-Plus、JWT、Vue 这些技术落地。

### Q3. 你在这个项目里遇到的最大困难是什么?怎么解决的?
> 挑一个**真实**的讲,下面三个任选,重点讲「定位 - 解决 - 收获」:
> 1. **Spring Boot 4 的 Jackson 迁移**:Spring Boot 4 默认用 Jackson 3,包名从 `com.fasterxml.jackson` 换成了 `tools.jackson`,很多老教程代码直接编译报错。我通过 `javap` 反查了 Jackson 3 的 API,把几个类的 import 全部迁过去,但注解还是 2.x 的包,这个细节容易踩坑。收获:遇到框架大版本升级,要去看官方迁移文档和实际 API,不能照搬旧代码。
> 2. **AI 输出不稳定**:DeepSeek 有时返回的不是标准 JSON。我加了 `response_format: {type:"json_object"}` 强制 JSON 模式,再用 Jackson 解析 + 字段校验,解析失败就抛业务异常,不让脏数据入库。收获:AI 接入的工程化关键在于「约束输出 + 容错」。
> 3. **密钥安全**:一开始把 MySQL 密码和 JWT 密钥硬编码在配置文件里,后来意识到不能提交到 GitHub。改成环境变量占位 + 本地 `application-local.yml`(gitignore)的方式。收获:敏感信息管理是基本工程素养。

### Q4. 这个项目还有什么可以改进的地方?
> (展示你有思考,别说完美)
> - 引入 Redis 做缓存和「考试倒计时」的分布式支持;
> - AI 调用目前是同步的,量大可以改成异步 + 重试 + 限流;
> - 主观题判分可以加「双模型交叉校验」提升准确度;
> - 加单元测试和接口文档(Swagger/OpenAPI);
> - 题目数据可以引入向量检索,做更精准的相似题推荐。

### Q5. 你的职业规划 / 期望薪资 / 到岗时间
> 按真实情况答即可,这部分是 HR 判断稳定性的,别紧张。

---

## 三、技术面试(考察原理与深度)

### Q1. 项目的整体架构是什么?分层怎么设计的?
> 前后端分离。后端分层:**Controller(接口层)→ Service(业务层)→ Mapper(MyBatis-Plus 持久层)**,另加 `common`(统一返回体/异常处理)、`security`(JWT 认证)、`ai`(DeepSeek 调用)、`dto`(请求响应对象)。
> 所有接口统一返回 `Result<T>{code, message, data}`,全局异常处理器兜底,登录外的接口都经过 `AuthInterceptor` 校验 JWT。

### Q2. 为什么用 JWT 而不是 Session?
> - **无状态**:服务端不存会话,横向扩展容易;
> - 前后端分离场景下,前端把 token 放 header,天然适配;
> - 缺点也有:token 一旦签发就无法主动失效,所以有效期设短 + 密码用 BCrypt,关键信息不敏感时够用。
> 我用的是 jjwt 库,HS384 签名,密钥从环境变量读,`Keys.hmacShaKeyFor` 生成密钥。

### Q3. 密码是怎么存的?为什么不能用 MD5?
> 用 **BCrypt**(spring-security-crypto)。MD5/SHA 是快哈希,能暴力枚举和彩虹表碰撞;BCrypt 自带**盐(salt)**和**慢哈希(可调 cost)**,专门设计来抗暴力破解。每次登录用 `BCrypt.matches(明文, 存库的哈希)` 比对,而不是「解密」。

### Q4. 雪花 ID 是什么?为什么用?
> 雪花算法生成的 64 位分布式唯一 ID:时间戳 + 机器 ID + 序列号,**趋势递增、全局唯一、不依赖数据库自增**。我通过 MyBatis-Plus 的 `IdType.ASSIGN_ID` 自动生成。好处是分库分表时不会冲突,也不会暴露业务量。

### Q5. 逻辑删除是什么?怎么实现的?
> 物理删除是 `DELETE` 真的删行;逻辑删除是给表加一个 `deleted` 字段(0 正常 / 1 已删),删除时只改成 1。MyBatis-Plus 用 `@TableLogic` 注解,查询时自动加 `WHERE deleted=0`,删除时自动改成 update。好处:数据可恢复、有审计痕迹,误删能找回。

### Q6. 你如何调用 DeepSeek?返回的 JSON 怎么保证格式正确?
> 用 Spring Boot 自带的 `RestClient`,POST 到 `https://api.deepseek.com/chat/completions`。几个关键点:
> - **强制 JSON**:请求体带 `response_format: {type:"json_object"}`,让模型只输出 JSON;
> - **提示词定 schema**:在 system 提示词里明确告诉模型要输出的 JSON 字段结构;
> - **解析 + 校验**:用 Jackson 反序列化成 `JsonNode`,逐个字段 `path()` 取值,字段缺失就抛业务异常,由全局异常处理器统一返回。

### Q7. 客观题和主观题是怎么判分的?
> - **客观题**(单选/多选/判断):本地规则判分,拿用户答案和正确答案比对,零成本、零延迟、可离线;
> - **主观题**(填空/简答):交给 DeepSeek,把「题目 + 参考答案 + 用户答案」拼进提示词,让它输出 `{score, comment}`,再解析回来存库。这样既省钱(简单题不调 AI),又能覆盖需要语义理解的题目。

### Q8. 个性化推荐是怎么做的?
> 基于**错题本统计**:把用户的错题按「知识点」分组,统计每个知识点的错误次数,找出**薄弱知识点**,再从题库里抽这些知识点的题目推荐给用户。本质是「错题 → 知识点 → 推荐题」的闭环,不依赖复杂算法,但效果好、可解释。

### Q9. 数据库是怎么设计的?为什么是这些表?
> 8 张表,围绕三个场景:
> - 用户:`user`(账号 + BCrypt 密码);
> - 题目域:`question`(题目本体,选项用 JSON 存)、`wrong_question`(错题本)、`practice_record`(刷题记录);
> - 考试域:`exam`(试卷)、`exam_question`(试卷-题目多对多)、`exam_record`(考试记录)、`exam_answer`(作答明细)。
> 关键设计:**试卷和题目是多对多**,所以中间表 `exam_question` 存 `exam_id + question_id + score + sort`;错题本单独成表,支撑推荐和统计;通用字段统一加 `create_time / update_time / deleted`。

### Q10. 你项目里 Spring Boot 4 和普通 2.x/3.x 有什么区别?
> 重点是两条:
> - **Java 21 起步**(Spring Boot 4 硬性要求);
> - **Jackson 3**:包名 `tools.jackson`,API 有变化,老代码要迁移。
> 另外 MyBatis-Plus 在 Spring Boot 4 下要用 `mybatis-plus-spring-boot4-starter`(专用的 boot4 starter)。这些都属于「框架大版本升级要重新核对依赖」的经验。

### Q11. 如果接口被频繁调用 / AI 接口很慢怎么办?
> - 前端/网关限流,后端对 AI 接口做**并发控制**和**超时**;
> - 客观题判分本地做,只有主观题才调 AI,减少慢请求占比;
> - 常用数据(题库、试卷)加 **Redis 缓存**;
> - AI 生成题目后**落库**,重复请求直接查库,不重复调 AI。

---

## 四、可能被追问的「进阶」题(知道思路即可)

- **为什么不用 Redis?** 现在单机 + 数据量小,MySQL 够用;引入 Redis 属于过度设计,但它是我下一步的优化方向。
- **接口安全怎么保证?** JWT 做身份认证、BCrypt 存密码、参数校验(Bean Validation)、全局异常不泄露堆栈、敏感信息走环境变量。
- **CORS 跨域怎么处理的?** 后端配 CORS 允许前端源;开发期前端 Vite 也能 `proxy` 代理 `/api` 到后端,联调不用处理跨域。
- **一致性怎么保证?** 交卷是一个事务:算分、更新考试记录、写作答明细、错题入错题本,要么全成功要么回滚(Spring `@Transactional`)。

---

## 五、高频加分点(主动说这些)

1. **手写 AI 调用**,不用框架,展示你对 HTTP 和 OpenAI 协议的理解;
2. **混合判分**,客观本地 + 主观 AI,展示成本意识;
3. **错题 → 知识点 → 推荐**的闭环,展示产品思维;
4. **敏感信息环境变量化 + gitignore**,展示工程素养;
5. **Spring Boot 4 / Jackson 3** 这类前沿版本踩坑经验,展示学习能力。

---

## 六、面试前自查清单

- [ ] 能不看稿,把三大场景讲清楚(一句话 + 每个场景做了什么)
- [ ] 能画出架构图和 8 张表的关系
- [ ] 能解释 JWT / BCrypt / 雪花 ID / 逻辑删除 四个点
- [ ] 能讲清「AI 判分」和「个性化推荐」两个核心逻辑
- [ ] 准备好 2~3 个「遇到的困难」的真实故事
- [ ] 项目代码已经能本地跑起来,能当场演示
