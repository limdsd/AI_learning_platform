# 简历项目经历 —— 书写方式(对齐你给的电商项目格式)

> 下面按你提供的「微服务电商平台」那种 **bullet 要点式** 风格,把你的 AI 智能学习平台重新写一版,直接抄进简历即可。

---

## ⚠️ 先看清一个关键区别(避免面试穿帮)

你给的电商项目是 **微服务 + 高并发** 方向,所以简历里出现了「Nacos、Feign、Gateway、Seata 分布式事务、RabbitMQ」这些词。

但你的 AI 学习平台是 **单体架构 + AI 驱动** 方向,技术上**没有**微服务那套东西。所以简历要**扬长避短**:

| | 电商项目(你给的例子) | 你的 AI 学习平台 |
|---|---|---|
| 主打 | 微服务、分布式、高并发 | AI 集成、智能出题、混合判分 |
| 关键词 | Nacos / Feign / Seata / MQ | DeepSeek / RestClient / 提示词 / 判分 |

**结论:别往简历里套微服务词**,否则面试官一追问「你的 Nacos 配置了什么」就露馅。你的亮点是 **AI 能力 + 后端工程规范**,重点写这两块。

---

## 一、标准版(后端岗 / 应届,推荐直接用)

```
项目描述:一个基于 AI 大模型驱动的智能学习平台,涵盖用户注册登录、智能生成题目、
智能刷题(错题本、AI 解析、个性化推荐)、智能考试(AI 组卷、自动判分、成绩报告)等
完整学习流程,致力于提供个性化、智能化的学习体验。

- 使用 SpringBoot 4 + MyBatis-Plus + MySQL 8 + Vue 3 + Element Plus + DeepSeek 等完成项目整体开发。
- 使用 JWT + BCrypt 实现用户注册登录与无状态认证,Spring 拦截器统一校验登录态,保障接口安全。
- 使用 Spring RestClient 直连 DeepSeek 的 OpenAI 兼容接口(未依赖第三方 AI 框架),实现 AI 智能出题、主观题判分、错题智能解析。
- 通过 response_format 强制 JSON 输出 + Jackson 反序列化 + 字段校验,解决大模型输出不稳定的容错问题,防止脏数据入库。
- 采用混合判分策略:客观题(单选/多选/判断)本地规则判分零成本,主观题(填空/简答)交由 AI 判分并生成评语。
- 基于错题本统计薄弱知识点,实现个性化题目推荐,形成「错题 → 知识点 → 推荐」的闭环。
- 使用 MyBatis-Plus 实现雪花 ID 主键、逻辑删除、分页查询;敏感信息通过环境变量 + 本地配置文件管理,保障工程安全规范。
```

---

## 二、全栈版(加了前端,投全栈岗位用)

在标准版基础上,把最后一条替换成 / 增加一条前端:

```
- 使用 Vue 3 + Pinia + Vue Router 搭建单页应用,封装 Axios 拦截器统一注入 JWT 与统一错误处理,
  完成登录、出题、刷题、考试作答(倒计时/题卡导航)、成绩报告等页面。
```

---

## 三、精简版(简历篇幅紧张,压成 3~4 行)

```
项目描述:AI 驱动的前后端分离智能学习平台,涵盖 AI 出题、智能刷题、智能考试三大场景。

- 技术栈:SpringBoot 4 + MyBatis-Plus + MySQL 8 + Vue 3 + Element Plus + DeepSeek。
- 手写 RestClient 直连 DeepSeek 实现 AI 出题、主观题判分、错题解析,并通过强制 JSON 输出做容错。
- 客观题本地判分、主观题 AI 判分的混合判分策略;基于错题统计做个性化推荐。
- JWT + BCrypt 认证,雪花 ID + 逻辑删除 + 分页,敏感信息环境变量化。
```

---

## 四、bullet 写作套路(自己改时照着套)

你给的电商项目每条 bullet 的句式是:**「使用/由/引入 XX + 动词 + 实现/完成 XX」**。套到你项目就是:

| 模板 | 你的项目对应写法 |
|---|---|
| 使用 XX 完成整体开发 | 使用 SpringBoot 4 + MyBatis-Plus + MySQL + Vue 3 + DeepSeek 完成整体开发 |
| 由 XX 统一管理 XX | 由 Spring 拦截器统一校验登录态,实现认证授权 |
| 使用 XX 实现 XX | 使用 RestClient 直连 DeepSeek 实现 AI 出题/判分 |
| 通过 XX 解决 XX 问题 | 通过强制 JSON 输出解决大模型输出不稳定问题 |
| 引入 XX 增强 XX | 引入混合判分策略,客观本地判分降低 AI 调用成本 |
| 使用 XX 缓存/提高性能 | (你的项目暂无缓存,可写) 本地判分避免频繁调 AI,提高响应速度 |

> 一句话:**每条 bullet = 技术名词 + 做了什么 + 带来什么**,不要只罗列名词。

---

## 五、对比:电商写法 vs 你的写法(看懂为什么)

**电商项目(微服务方向)的 bullet 逻辑:**
```
- 由 Nacos 统一管理配置,OpenFeign 远程调用       → 服务治理
- Spring Cloud Gateway 网关 + JWT 认证授权          → 网关鉴权
- Seata AT 模式解决分布式事务                       → 数据一致性
- RabbitMQ 异步消息解耦                            → 削峰解耦
- Redis 缓存热点数据                               → 性能优化
```

**你的项目(AI 方向)对应的 bullet 逻辑:**
```
- RestClient 直连 DeepSeek                         → AI 能力接入
- 强制 JSON + Jackson 解析 + 字段校验              → AI 输出容错
- 客观本地判分 + 主观 AI 判分                       → 混合判分降成本
- 错题按知识点统计推荐                              → 个性化闭环
- JWT + BCrypt + 环境变量                           → 安全规范
```

**核心心法:电商项目靠「微服务组件数量」取胜,你的项目靠「AI 集成深度 + 工程细节」取胜。** 别去拼自己没有的微服务,把你 AI 这条线讲深、讲透,一样能打。
